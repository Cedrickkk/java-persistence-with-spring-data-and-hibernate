spring:
  application:
    name: sharedprimarykey

  datasource:
    url: jdbc:postgresql://localhost:5433/ch9_advanced_mappings_onetoone_sharedprimarykey_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create